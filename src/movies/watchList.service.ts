import {
  BadRequestException,
  ForbiddenException,
  Injectable,
  Req,
} from '@nestjs/common';
@Injectable({})
// Chỗ nào cần thì sẽ inject được
export class WatchListService {
  constructor() {}
}
